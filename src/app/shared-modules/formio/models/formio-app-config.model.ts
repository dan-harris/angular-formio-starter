import { FormioAppConfig } from 'angular-formio';

/**
 * app config for formio
 */
export const formioAppConfig: FormioAppConfig = {
  appUrl: 'https://example.form.io',
  apiUrl: 'https://api.form.io'
};
